# OCR-Document-Processor
An intelligent OCR system that extracts text from documents, auto-detects only the relevant fields, fills them accurately, and outputs clean structured data with confidence scores.
